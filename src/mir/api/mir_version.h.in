#pragma once

#define mir_VERSION_STR "@mir_VERSION_STR@"
#define mir_VERSION     "@mir_VERSION@"

#define mir_VERSION_MAJOR @mir_VERSION_MAJOR@
#define mir_VERSION_MINOR @mir_VERSION_MINOR@
#define mir_VERSION_PATCH @mir_VERSION_PATCH@

const char * mir_version();

unsigned int mir_version_int();

const char * mir_version_str();

const char * mir_git_sha1();

