# MIR-239: check if intermediate grid is the same as the target grid, there is no "double" interpolation
# mars
param=t,level=1000
# mir
--intgrid=auto --gridname=F640
# plan
ShScalarToNamedGrid[ShToGridded=[cropping=none,options=[...]],gridname=F640]|Save[output=...]
