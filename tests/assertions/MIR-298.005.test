# Test mir::repres::gauss::reduced::Reduced::extendedBoundingBox, for areas that become periodic
# (for an intermediate reduced Gaussian grid, not the default behaviour)
# Note: also tested in mir_tests_unit_bounding_box
# MARS request
param=t,level=1000,resol=20
# mir
--grid=1/1 --area=90/10/-10/9 --intgrid=N160
# grib_get assertions
gridType=regular_ll
